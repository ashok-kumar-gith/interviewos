# Base URL of the InterviewOS backend. The typed client appends `/api/v1`.
# Local dev (direct to Go API):
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Behind nginx, use the public origin (e.g. https://app.interviewos.dev)
